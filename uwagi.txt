Ogólnie koncepcja jest dobra, ale należałoby się zastanowić jakie relacje mają być względem poszczególnych parametrów, bo teraz to wygląda trochę losowo -> czemu waga rośnie liniowo od pinga i strat, ale odwrotnie proporcjonalnie do B_wolne (jak zrobimy K_jak*M_wolne to mamy C_tech/B_wolne -> tracimy info o tym, czy to jest wolne 95 Mb/s na łączu 10Gb/s czy 100 Mb/s - może to bez różnicy?)
 
może loss się nie da, ale możemy zmierzyć np. ping pod kątem wartości średniej i odchylenia - duża zmienność = mniej stabilna sieć -> kara

są metody pomiaru dostępnej przepływności -> B_wolne
niech Pan szuka pod available bandwidth measurements

aktualnie speedtesty robią to "brutalnie" -> pobierają i wysyłają ile się da
ale są metody pociągu pakietów itd

nas chyba bardziej niż B_max / B_wolne interesuje coś w stylu B_wolne (które się może zmieniać) / wolumen generowanego ruchu (który w danej sieci mesh będzie pewnie stały)

no i my też będziemy te dane przesyłać, więc mamy na bieżąco "monitoring dostępnej przepływności" = speed z jakim wysyłamy (z dokładnością do tego, że pewnie nie wysyłamy jednego dużego pliku, tylko często małe, więc TCP może nie zdążyć się rozpędzić itd.)
nie wiem jak Pan chce ten U_eter policzyć, skąd to info o zajętości kanału

w tych wagach wewnętrznych też bym chyba założył, że znamy przepływności między poszczególnymi węzłami, ale musimy uwzględnić topologię

jak mam dwie takie topologie i każdy węzęł ma taki sam link "w górę", to wybrałbym jako relay te węzły centralne -> potrzeba najmniej hopów żeby przesłać wiadomość
a jeśli wybrałbym tego, to muszę wtedy każdą wiadomość przesłać przez środkowy 
Tutaj rozpisał taką gwiazdę, gdzie mamy centralny węzeł x i węzły a b c d,
chodzi o to że żeby premiować że jest do niego najkrótszy fragment, więc a b c i d najlepiej powinny wyslac po prostu do x, po prostu zeby on byl gatewayem bo wtedy przeslanie wiadomosci idzie po jednym linku, a w przeciwnym wypadku gdyby np d byl gatewayem to wtedy a b c musialy wyslac wiadomosci po dwoch hopach.

i nagle na tym łączu do cerwonego to ja muszę mieć w zasadzie 4x większą przepływność, bo tam idą wiadomości od 4 węzłów, dopóki to jest 4x 1 Mb/s a łącze ma 100 Mb/s - to nie ma żadnego problemu

Odpisalem mu: Zgadzam się natomiast chciałem to zrealizować na podstawie też metryk przepustowości i jakości połączęń na zewnątrz, gdyż np takie ustawienie miałoby uzasadnienie gdyby centralny węzeł miał słabe połączenie na zewnątrz
a on odpwoiedzial: tak, i tak robimy.

mamy 2 warunki i oba muszą być spełnione - wyjście w górę z węzła + dojście do węzła,
jak jeden węzeł nie jest w stanie transmitować, no to muszą dwa itd
To wszystkie jego uwagi poki co.